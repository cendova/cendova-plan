/// <reference types="vite/client" />

/** Zur Bauzeit eingebrannt (vite.config.ts) — Version aus package.json
 *  und der kurze Commit-Hash. Macht in der Oberflaeche sichtbar, welcher
 *  Stand laeuft. */
declare const __APP_VERSION__: string
declare const __APP_COMMIT__: string
